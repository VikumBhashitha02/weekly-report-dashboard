# Frontend Component Directory Structure

- `ui/`: Base reusable primitives (buttons, modals, inputs, badges)
- `common/`: Global shared components (Navbar, Sidebar, Footer, PageHeader)
- `reports/`: Weekly report forms, submission view, report cards
- `dashboard/`: Member and Manager metric widgets, summaries
- `charts/`: Recharts-based data visualizations
